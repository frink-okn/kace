apiVersion: v1
kind: Service
metadata:
  labels:
    app: frink-{{ kg_name }}-qlever-server
  name: frink-{{ kg_name }}-qlever-server
spec:
  ports:
  - name: tcp
    port: 7001
    protocol: TCP
    targetPort: 7001
  selector:
    app: frink-{{ kg_name }}-qlever-server
  type: ClusterIP
